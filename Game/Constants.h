#pragma once

const bool HIGH_RES = false;
// Screen width.
const int WIDTH  = HIGH_RES ? 1920 : 1280;
// Screen height.
const int HEIGHT = HIGH_RES ? 1080 : 720;
// OpenGL draw scaling.
const float SCALE = 100;